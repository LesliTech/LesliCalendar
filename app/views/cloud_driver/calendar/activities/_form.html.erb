<%= form_with(model: calendar_activity, local: true) do |form| %>
  <% if calendar_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_activity.errors.count, "error") %> prohibited this calendar_activity from being saved:</h2>

      <ul>
        <% calendar_activity.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
