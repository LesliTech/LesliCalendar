<%= form_with(model: calendar_subscriber, local: true) do |form| %>
  <% if calendar_subscriber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_subscriber.errors.count, "error") %> prohibited this calendar_subscriber from being saved:</h2>

      <ul>
        <% calendar_subscriber.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
