<%= form_with(model: calendar_attachment, local: true) do |form| %>
  <% if calendar_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_attachment.errors.count, "error") %> prohibited this calendar_attachment from being saved:</h2>

      <ul>
        <% calendar_attachment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
