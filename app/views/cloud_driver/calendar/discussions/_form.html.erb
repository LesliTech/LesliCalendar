<%= form_with(model: calendar_discussion, local: true) do |form| %>
  <% if calendar_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_discussion.errors.count, "error") %> prohibited this calendar_discussion from being saved:</h2>

      <ul>
        <% calendar_discussion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
