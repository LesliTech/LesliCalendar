<%= form_with(model: calendar_file, local: true) do |form| %>
  <% if calendar_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_file.errors.count, "error") %> prohibited this calendar_file from being saved:</h2>

      <ul>
        <% calendar_file.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
