<%= form_with(model: calendar_detail, local: true) do |form| %>
  <% if calendar_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_detail.errors.count, "error") %> prohibited this calendar_detail from being saved:</h2>

      <ul>
        <% calendar_detail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
