<%= form_with(model: calendar_comment, local: true) do |form| %>
  <% if calendar_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_comment.errors.count, "error") %> prohibited this calendar_comment from being saved:</h2>

      <ul>
        <% calendar_comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
