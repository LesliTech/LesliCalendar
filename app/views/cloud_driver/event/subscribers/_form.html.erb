<%= form_with(model: event_subscriber, local: true) do |form| %>
  <% if event_subscriber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_subscriber.errors.count, "error") %> prohibited this event_subscriber from being saved:</h2>

      <ul>
        <% event_subscriber.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
