<%= form_with(model: event_activity, local: true) do |form| %>
  <% if event_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_activity.errors.count, "error") %> prohibited this event_activity from being saved:</h2>

      <ul>
        <% event_activity.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
