<%= form_with(model: event_subscription, local: true) do |form| %>
  <% if event_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_subscription.errors.count, "error") %> prohibited this event_subscription from being saved:</h2>

      <ul>
        <% event_subscription.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
