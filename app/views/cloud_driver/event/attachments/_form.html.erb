<%= form_with(model: event_attachment, local: true) do |form| %>
  <% if event_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_attachment.errors.count, "error") %> prohibited this event_attachment from being saved:</h2>

      <ul>
        <% event_attachment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
