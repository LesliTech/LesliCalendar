<%= form_with(model: event_attendant, local: true) do |form| %>
  <% if event_attendant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_attendant.errors.count, "error") %> prohibited this event_attendant from being saved:</h2>

      <ul>
        <% event_attendant.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
