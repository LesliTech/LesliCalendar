<%= form_with(model: event_discussion, local: true) do |form| %>
  <% if event_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_discussion.errors.count, "error") %> prohibited this event_discussion from being saved:</h2>

      <ul>
        <% event_discussion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
