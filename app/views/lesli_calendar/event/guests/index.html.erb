<p style="color: green"><%= notice %></p>

<h1>Guests</h1>

<div id="event_guests">
  <% @event_guests.each do |event_guest| %>
    <%= render event_guest %>
    <p>
      <%= link_to "Show this guest", event_guest %>
    </p>
  <% end %>
</div>

<%= link_to "New guest", new_event_guest_path %>
